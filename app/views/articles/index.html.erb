Hello world
<table>
  <tr>
    <th>User_id</th>
    <th>Title</th>
    <th>Body</th>
  </tr>
  <% @articles.each do |article| %>
  <tr>
      <td><%= article.user_id %></td>
      <td><%= article.title %></td>
      <td><%= article.body %></td>
  </tr>
  <% end %>

</table>
